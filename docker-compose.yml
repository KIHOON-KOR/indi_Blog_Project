services:
  # ---------------------------------------------------------------------------
  # 1. Django 백엔드 (개발용)
  # ---------------------------------------------------------------------------
  backend:
    build: .
    container_name: blog_backend      # [변경] 컨테이너 이름을 직관적으로 변경
    volumes:
      - ./:/app
      # - static_volume:/app/staticfiles
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # [중요] DB 접속 호스트는 아래 db 서비스의 'container_name'과 같아야 합니다.
      - POSTGRES_HOST=blog_db
    depends_on:
      - db
    networks:
      - app_network
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"

  # ---------------------------------------------------------------------------
  # 2. PostgreSQL 데이터베이스
  # ---------------------------------------------------------------------------
  db:
    image: postgres:15
    container_name: blog_db           # [변경] DB 컨테이너 이름 변경
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env
    networks:
      - app_network

  # ---------------------------------------------------------------------------
  # 3. Redis 캐시 서버 (이메일 인증번호 저장용)
  # ---------------------------------------------------------------------------
  redis:                              # 새롭게 띄울 Redis 서비스의 이름입니다.
    image: redis:7                    # 도커 허브에서 공식 Redis 7버전 이미지를 가져와 사용합니다.
    container_name: blog_redis        # 생성될 컨테이너의 이름을 'blog_redis'로 지정합니다.
    ports:
      - "6379:6379"                   # 외부(호스트)의 6379 포트와 내부(컨테이너)의 6379 포트를 연결합니다.
    networks:
      - app_network                   # backend, db와 같은 네트워크 망에 묶어주어 서로 통신할 수 있게 합니다.

  # ---------------------------------------------------------------------------
  # [주석 처리] 나중에 주석 풀 때도 이름이 맞도록 수정해 두었습니다.
  # ---------------------------------------------------------------------------

  # nginx:
  #   build: ./nginx
  #   container_name: blog_nginx      # [변경] Nginx 컨테이너 이름
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - static_volume:/app/staticfiles
  #     - media_volume:/app/media
  #   depends_on:
  #     - backend
  #   networks:
  #     - app_network
  #   restart: unless-stopped

  # celery_worker:
  #     build: .
  #     container_name: blog_celery     # [변경] Celery 컨테이너 이름
  #     command: celery -A config worker --loglevel=info
  #     volumes:
  #       - ./:/app
  #     env_file:
  #       - .env
  #     environment:
  #       - POSTGRES_HOST=blog_db       # [변경] 위에서 설정한 DB 이름과 일치
  #       - REDIS_URL=redis://blog_redis:6379/0  # [변경] redis 컨테이너 이름과 일치
  #     depends_on:
  #       - redis
  #       - db
  #     networks:
  #       - app_network
  #     restart: always

volumes:
  postgres_data:
  # static_volume:
  # media_volume:

networks:
  app_network:
    driver: bridge